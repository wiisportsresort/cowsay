import { CowFunction } from '../types';

export const owl: CowFunction = ({ thoughts }) => `
         ${thoughts}
          ${thoughts}
           ___
          (o o)
         (  V  )
        /--m-m-
`;
