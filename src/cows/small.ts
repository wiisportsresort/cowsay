import { CowFunction } from '../types';

export const small: CowFunction = ({ thoughts, eyes, tongue }) => `
       ${thoughts}   ,__,
        ${thoughts}  (${eyes})____
           (__)    )\\
            ${tongue}||--|| *
`;
