import { CowFunction } from '../types';

export const goat2: CowFunction = ({ thoughts, eyes, tongue }) => `
        ${thoughts}
         ${thoughts}
          )__(
         '|${eyes}|'________/
          |__|         |
           ${tongue}||"""""""||
             ||       ||

`;
