import { CowFunction } from '../types';

export const mailchimp: CowFunction = ({ thoughts }) => `
${thoughts}
 ${thoughts}
    ______
   / ___M ]__
C{ ( o o )}
    {     ••
      \\___
      ----´
`;
